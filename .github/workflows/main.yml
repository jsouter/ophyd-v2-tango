on: [push]

jobs:
#   test:
#     runs-on: ubuntu-latest
#     container:
#       image: ghcr.io/jsouter/alpine:latest
#       credentials:
#         username: ${{ github.actor }}
#         password: ${{ secrets.GITHUB_TOKEN }}
#     steps:
#       - name: say hello
#         run: echo hello
  sardana:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/jsouter/ophyd-v2-tango/sardemo:v2
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: checkout repo
        uses: actions/checkout@v3.0.2
      - name: start sardana
        run: |
          /startsard.sh
          python3 tango_devices.py

